name: Reusable CI Pipeline

on:
  workflow_call:
    inputs:
      service_name:
        required: true
        type: string
      ecr_repository:
        required: true
        type: string
    secrets:
      SONAR_TOKEN:
        required: true
      SNYK_TOKEN:
        required: false
      AWS_ROLE_ARN:
        required: true
      GITOPS_PAT:
        required: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm ci

      - name: Lint
        run: npm run lint --if-present

      - name: Unit Tests
        run: npm test --if-present

      - name: SonarCloud Scan
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          echo "Setting up SonarScanner..."
          mkdir -p $HOME/.sonar
          if curl -sSLo $HOME/.sonar/sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-7.0.2.4839-linux-x64.zip; then
            unzip -q -o $HOME/.sonar/sonar-scanner.zip -d $HOME/.sonar/
            export PATH="$HOME/.sonar/sonar-scanner-7.0.2.4839-linux-x64/bin:$PATH"
            
            echo "Running SonarCloud Scan..."
            if sonar-scanner -Dsonar.host.url="https://sonarcloud.io" > /tmp/sonar.log 2>&1; then
              echo "SonarCloud Scan completed successfully."
              cat /tmp/sonar.log
            else
              echo "add quality gate in the sonarcloud"
            fi
          else
            echo "Failed to setup SonarScanner. add quality gate in the sonarcloud"
          fi

      - name: Snyk Security Scan
        if: ${{ env.SNYK_TOKEN != '' }}
        continue-on-error: true
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build Docker Image
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          REPOSITORY: ${{ inputs.ecr_repository }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .

      - name: Trivy Image Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ steps.login-ecr.outputs.registry }}/${{ inputs.ecr_repository }}:${{ github.sha }}'
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'

      - name: Push Docker Image
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/feature/test'
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          REPOSITORY: ${{ inputs.ecr_repository }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG

      - name: Checkout GitOps Repo
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/feature/test'
        uses: actions/checkout@v4
        with:
          repository: AWS-Elderping/elderping-gitops
          token: ${{ secrets.GITOPS_PAT }}
          ref: ${{ github.ref_name }}
          path: gitops-repo

      - name: Setup yq
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/feature/test'
        uses: mikefarah/yq@v4.44.2

      - name: Update Helm Values in GitOps
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/feature/test'
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            ENV="prod"
          elif [ "${{ github.ref_name }}" = "develop" ]; then
            ENV="dev"
          elif [ "${{ github.ref_name }}" = "feature/test" ]; then
            ENV="stage"
          else
            echo "Unsupported branch: ${{ github.ref_name }}"
            exit 1
          fi
          
          VALUES_FILE="gitops-repo/environments/${ENV}/${{ inputs.service_name }}-values.yaml"
          echo "Updating values file: $VALUES_FILE with tag ${{ github.sha }}"
          
          if [ -f "$VALUES_FILE" ]; then
            yq e -i ".image.tag = \"${{ github.sha }}\"" "$VALUES_FILE"
          else
            echo "Values file not found: $VALUES_FILE"
            exit 1
          fi

      - name: Commit and Push to GitOps
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/feature/test'
        run: |
          cd gitops-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add environments/
          if git diff --staged --quiet; then
            echo "No changes to deploy (image tag is already up to date)."
          else
            git commit -m "deploy: update ${{ inputs.service_name }} image tag to ${{ github.sha }} [skip ci]"
            git push origin HEAD
          fi

